import 'package:flutter/material.dart';

class PrintHistoryView extends StatelessWidget {
  const PrintHistoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Historial de Impresiones'),
    );
  }
}